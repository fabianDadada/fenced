Provides deterministic values exclusively for automated testing.

Use this skill when verifying end-to-end flows in development or CI. It never touches external systems and always returns the same static payloads so assertions remain stable.
