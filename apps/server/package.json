{
  "name": "@fenced/server",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun run index.ts",
    "build": "bun build ./index.ts --target bun --outdir dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@fenced/channel": "workspace:*",
    "@fenced/runtime": "workspace:*",
    "@fenced/session": "workspace:*",
    "@fenced/shared": "workspace:*",
    "react": "^19.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.3.5",
    "typescript": "^5.9.3"
  }
}
